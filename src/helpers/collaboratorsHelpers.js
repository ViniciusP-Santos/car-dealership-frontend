export const nivelFormat = type => {
    if(type === 'admin'){
      return 'Admin'
    }
    if(type === 'salesperson'){
      return 'Vendedor'
    }
    if(type === 'marketing'){
      return 'Marketing'
    }
}
  